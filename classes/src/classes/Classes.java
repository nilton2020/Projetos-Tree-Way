package classes;

public class Classes {

	public static void main(String[] args) {

		int inteira;
		byte tamanho; ///É o tipo de dado capaz de armazenar 8 bits de informação, 
//		ou seja, um número inteiro entre -128 e 127. Sua utilização é recomendada em caso 
//		de economia de memória, já que alguns tipos de dados maiores possuem processamento mais rápido.
//		
		long bwa; ///É o tipo de dado capaz de armazenar 64 bits de informação, ou seja, que pode 
//		representar um número inteiro qualquer entre -9.223.372.036.854.775.808L e 9.223.372.036.854.775.807L. 
//		Recomenda-se seu uso apenas quando for preciso assumir valores maiores ou menores do que aqueles 
//		possíveis de serem assumidos pelo int.
//		
		
		short by1; /// É o tipo de dado que é capaz de armazenar números inteiros de 16 bits, ou seja, 
		///um número inteiro entre -32.768 e 32.767.
		
		char letra; ///É o tipo de dado capaz de armazenar 16 bits representando caracteres no formato UTF-16 
//		(formato UTF composto de dois caracteres de 8 bits). 
//		Representado numericamente, o tipo char pode ter valores de 0 a 65535 (inclusive)
//		- de '\u0000' a '\uffff'. Nas versões mais recentes da linguagem Java, propõe-se 
//		substituir o tipo char pelo tipo byte.
//		
		
		boolean certoErrado; ///É o tipo de dado que contém literal lógico. Serve para armazenar um único bit de informação. 
//		Este bit pode ser representado pelas palavras false (falso) ou true (verdadeiro). Representa estados binários, tais como:
//		verdadeiro/falso, certo/errado, ligado/desligado, aberto/fechado, sim/não etc
//		
		char letr;/// É o tipo de dado capaz de armazenar 16 bits representando caracteres no formato UTF-16 
//		(formato UTF composto de dois caracteres de 8 bits). Representado numericamente, o tipo char pode ter 
//		valores de 0 a 65535 (inclusive) - de '\u0000' a '\uffff'. Nas versões mais recentes da linguagem Java, 
//		propõe-se substituir o tipo char pelo tipo byte.
//		
		float dinheiro; ///É o tipo de dado capaz de armazenar números reais de precisão simples, ou seja, 
		///32 bits de informação representando um número real.
		
		double by3; ///É o tipo de dado capaz de armazenar números reais de precisão dupla, ou seja, 64 bits de informação em forma
//		de número real. É usado para representar valores nos quais é preciso uma precisão maior que a de float.

		String texto;
		
		Integer interagir;
		Boolean identificador;
		null pelado;
		
		System.out.print(int + byte + long + short + char + boolean + float + double + String + Intereger + Boolean + );
	}

}
